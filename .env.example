# ==============================================================================
# PagaaierTools Environment Configuration
# ==============================================================================
# Copy this file to .env and update with your actual values
# Never commit .env to version control!

# ==============================================================================
# PORT CONFIGURATION
# ==============================================================================
# Port assignments generated by VSFileTools Port Calculator
# These ports are deterministically generated based on project name

# Main application port (web server)
PORT=9344

# Service-specific ports (for future expansion)
PORT_FRONTEND=9343
PORT_BACKEND=9344
PORT_API=9345
PORT_DOCS=9346

# ==============================================================================
# SECURITY CONFIGURATION
# ==============================================================================
# Session secret - Use a strong random string (min 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-random-secret-here-min-32-chars-change-this

# Admin credentials (change these!)
ADMIN_USERNAME=brecht
ADMIN_PASSWORD=He33e-8620_;

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================
# Node environment: development, production, test
NODE_ENV=production

# Logging level: error, warn, info, debug
LOG_LEVEL=info

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
# SQLite database file location
DB_PATH=./portaal.db

# Backup directory
BACKUP_DIR=./backups

# ==============================================================================
# SERVER CONFIGURATION
# ==============================================================================
# Session cookie settings
SESSION_MAX_AGE=86400000
# 24 hours in milliseconds

# Request timeout (milliseconds)
REQUEST_TIMEOUT=30000

# ==============================================================================
# NETWORK CONFIGURATION
# ==============================================================================
# Default host for port checking
DEFAULT_HOST=localhost

# Port check timeout (milliseconds)
PORT_CHECK_TIMEOUT=1000

# Server network addresses
# LAN IP - Local network access (current device's LAN IP)
SERVER_LAN_IP=10.46.54.180
# VPN IP - VPN network access (WireGuard/OpenVPN address)
SERVER_VPN_IP=10.0.0.9

# URL Display Settings (true/false)
# Show localhost URLs (http://localhost:port) - for local server access
SHOW_LOCALHOST_URLS=true
# Show LAN IP URLs (http://10.46.54.180:port) - for local network access
SHOW_LAN_URLS=true
# Show VPN IP URLs (http://10.0.0.9:port) - for VPN access
SHOW_VPN_URLS=true
# Show offline projects in the project list
SHOW_OFFLINE_PROJECTS=true

# ==============================================================================
# PROJECT DEFAULTS
# ==============================================================================
# Default port for new projects (if not detected)
DEFAULT_PROJECT_PORT=3000
